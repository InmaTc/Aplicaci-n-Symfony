{% extends 'base.html.twig' %}

{% block title %}Modificar empleado{% endblock %}

{% block body %}
    {% if empleado.id %}
        <h1>Modificando a {{ empleado }}</h1>
    {% else %}
        <h1>Nuevo empleado</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">
        {% if empleado.id %}
            <i class="fa fa-check"></i> Guardar cambios
        {% else %}
            <i class="fa fa-plus"></i> Crear nuevo empleado
        {% endif %}
    </button>
    <a href="{{ path('empleado_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver al listado</a>
    {{ form_end(form) }}

{% endblock %}
