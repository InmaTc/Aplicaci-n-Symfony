{% extends 'base.html.twig' %}

{% block title %}
    Eliminar empleado
{% endblock %}


{% block body %}
    <h1>Eliminación de {{ empleado }}</h1>

    <div class="alert alert-danger">
        ¡Aviso! Se dispone a eliminar a {{ empleado }}. Esta operación
        no se puede deshacer.
    </div>
    <form method="post">
        <button type="submit" name="confirmar" value="si" class="btn btn-danger"><i class="fa fa-trash-alt"></i> Eliminar definitivamente</button>
        <a href="{{ path('empleado_listar') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver al listado sin eliminar</a>
    </form>
{% endblock %}

